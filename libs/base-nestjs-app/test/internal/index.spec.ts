import * as internal from '../../src/internal';

describe('internal index', () => {
	it('should export expected members', () => {
		expect(internal).toBeDefined();
		// Add more specific checks if you know what is exported
	});
});
