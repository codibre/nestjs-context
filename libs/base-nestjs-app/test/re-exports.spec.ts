import * as reExports from '../src/re-exports';

describe('re-exports', () => {
	it('should export expected members', () => {
		expect(reExports).toBeDefined();
		// Add more specific checks if you know what is re-exported
	});
});
